This edit used the built-in image generation tool on the original architecture PNG.
The output is `previewforge-architecture-v2.png`; the original is preserved.

Use case: text-localization.
Asset type: PreviewForge project architecture diagram for README and LinkedIn.
Input image: edit target, the existing project architecture PNG.
Make only these exact text corrections:
1. Replace the subtitle under PreviewForge with "Project architecture · One platform repository · Local PR environments".
2. In the top-left HickoDev / PreviewForge box replace "Private repository" with "Platform repository".
3. In GHCR ensure the two small lines read exactly "Private, immutable images" and "Pulled by kind nodes". Spell immutable correctly.
Preserve everything else: exact canvas aspect ratio, all boxes, icons, layout, color, borders, connectors and arrow directions, all other wording, local ports, Floci placement outside kind, NVIDIA hosted inference, AI pending-quality footnote. Do not add elements, arrows, extra slogans, watermarks, or expose credentials. This is a surgical text edit, not a redesign. The source repository can be private or public; GHCR package remains private.
