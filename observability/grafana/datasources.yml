apiVersion: 1
datasources:
  - name: Prometheus
    uid: previewforge-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus.observability.svc.cluster.local:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 5s
